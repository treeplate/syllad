// Expected output: PASS

StringList cwdParts = split(args[0], '\\');
pop(cwdParts);
if (len(cwdParts) == 0) {
  append(cwdParts, '.');
}
String cwd = join(cwdParts, '/');
String file = readFile(concat(cwd, '/readfile.txt'));

println(file);