// expected error: error: integer literal too big
// unexpected output: FAIL

println('FAIL');

// (currently buggy in compiler -- no compile time error is reported)

Integer n = 0xFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF;
if (n == 0xFFFFFFFFFFFFFFFF) {
  print("ERROR\n");
}