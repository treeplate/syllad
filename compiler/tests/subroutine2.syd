// should print "<function (foo function)>" or other implementation-specific toString
// currently fails because pushing reference to subroutine gets wrong assembler
// currently fails because subroutines don't type check arguments
// currently fails because internal "println" does not type check args
// currently fails because "println" is not implemented as stringifying
// currently fails because no stringification of function references

Null foo() {}
println(foo);